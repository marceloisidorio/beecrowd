#include <stdio.h>
 
int main() {
    int A, B, PROD;
    
    scanf("%d %d", &A, &B); // recebe os valores
    
	PROD = A*B; // realiza o cálculo
    
    printf("PROD = %d\n", PROD); // apresenta o resultado
    
    return 0;
}
