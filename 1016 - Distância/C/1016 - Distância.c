#include <stdio.h>

int main() {
	int Km; // declaração de variáveis
	
	scanf("%d", &Km); // recebe dados de entrada
	
	printf("%d minutos\n", Km*2); // apresenta o resultado + operação
	
	return 0;
}
